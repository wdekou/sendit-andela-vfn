dist: trusty
language: node_js
branches:
  only:
    - develop
cache:
  yarn: true
before_install:
  - curl -o- -L https://yarnpkg.com/install.sh | bash -s -- --nightly
  - export PATH="$HOME/.yarn/bin:$PATH"
install: true
script:
  - yarn lint
  - yarn test
